name: Update job/post counters

on:
  schedule:
    - cron: "*/30 * * * *"   # каждые 30 минут
  workflow_dispatch:         # позволит запускать вручную

permissions:
  contents: write            # нужно, чтобы записывать JSON в репозиторий

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Compute counters
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          HDRS="-H 'Authorization: Bearer ${GH_TOKEN}' -H 'Accept: application/vnd.github+json'"

          # считаем ПУБЛИЧНЫЕ репозитории пользователя GuitarEmo с кусочком "job" и "post" в названии
          JOBS=$(bash -lc "curl -s https://api.github.com/search/repositories?q=user:GuitarEmo+in:name+job ${HDRS} | jq -r .total_count")
          POSTS=$(bash -lc "curl -s https://api.github.com/search/repositories?q=user:GuitarEmo+in:name+post ${HDRS} | jq -r .total_count")

          mkdir -p badges

          cat > badges/job-count.json <<JSON
          { "schemaVersion": 1, "label": "💼 Открытые вакансии", "message": "${JOBS:-0}", "color": "00D0BF" }
          JSON

          cat > badges/post-count.json <<JSON
          { "schemaVersion": 1, "label": "📝 Полезные посты", "message": "${POSTS:-0}", "color": "00D0BF" }
          JSON

      - name: Commit & Push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add badges/*.json
          git commit -m "chore: update job/post counters" || echo "No changes"
          git push
